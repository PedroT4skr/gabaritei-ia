// ===== Placeholder — lib/imageProcessor.ts =====
// Será implementado na Fase 3 (OMR)
// Pipeline: captura → grayscale → threshold → detecção de bolhas

export {};

// TODO: Implementar na Fase 3
// - convertToGrayscale(uri: string): Promise<string>
// - adaptiveThreshold(uri: string): Promise<string>
// - detectBubbles(uri: string, config: BubbleGridConfig): Promise<DetectedBubble[]>
// - identifyFilledBubbles(bubbles: DetectedBubble[]): Promise<FilledBubble[]>
